# Copy to .env and fill in values. Never commit .env.
PORT=3000

# Rate limiter config
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10

# Redis (if using Redis store instead of in-memory)
# REDIS_URL=redis://localhost:6379
